# WinSettingsGui

Tray-приложение для Windows 11 для быстрого управления настройками питания (экран, сон, гибернация) через вложенное контекстное меню.

## Возможности

- Управление таймаутами отключения экрана (AC/DC)
- Управление таймаутами перехода в сон (AC/DC)
- Управление таймаутами гибернации (AC/DC)
- Автоопределение текущих системных настроек при запуске (галочки в меню соответствуют реальным значениям)
- Настраиваемые значения таймаутов через диалог настроек (применяются без перезапуска)
- Конфигурация сохраняется в `%APPDATA%/WinSettingsGui/config.json`
- Автозапуск при старте Windows (через реестр)
- Планирование изменений настроек по расписанию (дни недели, время, несколько действий)
- Иконка в системном трее с вложенными меню
- Поддержка русской и английской локализации Windows (парсинг powercfg)

## Структура меню

```
Настройки питания
├── Экран
│   ├── При питании от сети
│   │   ├── Отключать через 1 мин  [✓]
│   │   ├── Отключать через 5 мин
│   │   ├── Отключать через 15 мин
│   │   ├── Отключать через 30 мин
│   │   └── Не отключать экран
│   └── При питании от батареи
│       └── (аналогично)
├── Спящий режим
│   └── (аналогично)
└── Гибернация
    └── (аналогично)
───────────
Планирование      → расписание изменений настроек
Настройки         → диалог настройки таймаутов
Автозапуск    [✓] → переключатель
О программе       → информация
───────────
Выход
```

## Технологии

| Назначение | Библиотека |
|---|---|
| Системный трей | `github.com/energye/systray` |
| Диалог настроек | Win32 API (CreateWindowEx) |
| Диалог "О программе" | `MessageBoxW` через `syscall` |
| Автозапуск | `golang.org/x/sys/windows/registry` |
| Управление питанием | `powercfg` через `os/exec` |

## Структура проекта

```
winSettingsGui/
├── main.go                          -- Точка входа
├── go.mod / go.sum
├── resources/
│   ├── app.ico                      -- Иконка приложения
│   └── app.manifest                 -- Win32 манифест
├── internal/
│   ├── config/
│   │   └── config.go                -- Конфиг: загрузка/сохранение JSON в %APPDATA%
│   ├── power/
│   │   └── power.go                 -- Чтение/установка powercfg
│   ├── tray/
│   │   └── tray.go                  -- Трей-иконка, построение меню, обработка событий
│   ├── dialog/
│   │   ├── settings.go              -- Окно настроек (Win32 API)
│   │   ├── schedule.go              -- Список запланированных заданий
│   │   ├── schedule_edit.go         -- Редактирование задания
│   │   └── about.go                 -- Окно "О программе" (MessageBox)
│   ├── scheduler/
│   │   └── scheduler.go             -- Фоновый планировщик заданий
│   └── autostart/
│       └── autostart.go             -- Автозапуск через реестр
└── README.md
```

## Конфигурация

Конфиг хранится в `%APPDATA%/WinSettingsGui/config.json`. По умолчанию таймауты: `[1, 5, 15, 30]` минут для каждой категории. Запланированные задания также сохраняются в конфиге.

## Планирование

Через пункт меню "Планирование" можно создавать задания для автоматического изменения настроек:
- Выбор дней недели (Пн-Вс)
- Время выполнения (ЧЧ:ММ)
- Несколько действий в одном задании (экран/сон/гибернация × AC/DC × таймаут)
- Активация/деактивация без удаления
- Фоновый планировщик проверяет задания каждые 15 секунд
- Автообновление галочек в меню после срабатывания расписания

## Сборка

```bash
go build -ldflags "-H=windowsgui -s -w" -o WinSettingsGui.exe
```

## Использование

1. Запустить `WinSettingsGui.exe`
2. В системном трее появится иконка
3. ПКМ по иконке — контекстное меню с настройками питания
4. Выбор таймаута — мгновенное применение через `powercfg`

## Автор

Vladimir Iarovoi <me@iarovoivv.com>

Соавторство: Claude Code (Anthropic)
